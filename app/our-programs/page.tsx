import OurProgramsPage from "@/modules/our-programs";

/** Our Programs route — composed UI from `modules/our-programs` (thin page). */
export default function OurProgramsRoutePage() {
  return <OurProgramsPage />;
}
